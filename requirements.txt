# BacPipe 2.0 - Python runtime dependencies
# BSB (Basil Britto Xavier) - UMCG/DRAIGON Project
#
# Python version requirement is enforced via setup.py (python_requires=">=3.11").
# Optional extras (ai, dev, docs, gui) are declared in setup.py extras_require —
# install with: pip install -e ".[dev]" or ".[ai]"
# External bioinformatics binaries (fastp, multiqc, spades, flye, etc.) are
# installed via conda/bioconda — see scripts/install.sh.

# Async / concurrency
aiofiles>=0.8.0
aiohttp>=3.8.0

# Data science and bioinformatics
biopython>=1.80
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
scipy>=1.10.0

# Configuration and serialization
pyyaml>=6.0
toml>=0.10.0
click>=8.0.0
typer>=0.9.0

# Web framework
fastapi>=0.100.0
uvicorn>=0.22.0
websockets>=11.0.0

# Visualization
plotly>=5.15.0
matplotlib>=3.7.0
seaborn>=0.12.0

# File handling
openpyxl>=3.1.0
xlsxwriter>=3.1.0
reportlab>=4.0.0

# Logging / UX
loguru>=0.7.0
rich>=13.0.0
tqdm>=4.65.0

# Workflow / orchestration
snakemake>=7.30.0

# Database / network
sqlalchemy>=2.0.0
requests>=2.31.0
urllib3>=2.0.0

# Process / parallel
psutil>=5.9.0
joblib>=1.3.0
multiprocessing-logging>=0.3.0

# Containerization (Python client)
docker>=6.1.0

# Platform-specific
pywin32>=306;platform_system=="Windows"
python-magic>=0.4.0;platform_system=="Linux"
python-magic-bin>=0.4.0;platform_system=="Windows"
